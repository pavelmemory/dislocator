// Deprecated: the admin actions (file upload and signup-link creation) moved
// into the top-right GearMenu, together with logout. This thin re-export keeps
// any old import path working; it is safe to delete.
export { default } from './GearMenu';
